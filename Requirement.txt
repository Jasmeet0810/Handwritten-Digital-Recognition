# Core
numpy==1.24.3
matplotlib==3.7.1

# PyTorch (version auto-managed in Colab, but listed for local use)
torch==2.0.1
torchvision==0.15.2

# Jupyter/Colab
ipykernel==6.22.0
notebook==6.5.4

# Optional (for tqdm progress bars, if needed)
tqdm==4.65.0
